import { requireNativeComponent } from 'react-native';
import { AwesomeListViewProps } from './types';

const AwesomeListView = requireNativeComponent<AwesomeListViewProps>('AwesomeListView');

export  {AwesomeListView};
